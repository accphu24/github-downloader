storage/

# File APK/AAB build ra - không commit vào repo, chỉ lấy qua GitHub Actions artifact
*.apk
*.aab
*.ipa

# Thư mục android/ được CI (build_apk.yml) tự sinh mới mỗi lần chạy qua
# `flutter create`, không cần commit vào repo.
android/
ios/

# Rác build chuẩn của Flutter
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
